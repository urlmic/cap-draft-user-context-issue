{
  "name": "draft-user-context-demo",
  "version": "1.0.0",
  "description": "Minimal CAP app demonstrating draft user context issue",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^0.4.0",
    "@cap-js/sqlite": "^1",
    "jest": "^30.2.0"
  },
  "scripts": {
    "test": "jest"
  },
  "cds": {
    "requires": {
      "[development]": {
        "auth": {
          "strategy": "mock",
          "users": {
            "alice": {
              "password": "alice",
              "roles": [
                "admin"
              ]
            }
          }
        }
      }
    }
  }
}